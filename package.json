{
  "dependencies": {
    "emoji-picker-react": "^4.12.2"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "scripts": {
    "start": "concurrently \"cd public && npm start\" \"cd server && npm start\"",
    "build": "concurrently \"cd public && npm run build\" \"cd server && npm run build\"",
    "build:frontend": "cd public && npm run build",
    "build:backend": "cd server && npm run build"
  }
}
